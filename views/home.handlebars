<ol class="content">
    {{#each posts as |post|}}
    <li class="post">
        <div class="title">
            <a href="/post/{{post.id}}">{{post.title}}</a>
        </div>
        {{> post-details post}}
    </li>
    {{/each}}
</ol>